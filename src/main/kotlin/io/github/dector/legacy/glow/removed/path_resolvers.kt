package io.github.dector.legacy.glow.removed


/*fun indexPagePathResolver(pageNumber: Int) = "/${pageFileName(pageNumber)}.html"*/

/*fun postPagePathResolver(post: Post) = "/posts/${post.title.correctForPath()}.html"*/

/*fun tagPagePathResolver(tag: String, pageNumber: Int) = "/tags/$tag/${pageFileName(pageNumber)}.html"*/

/*
private fun String.correctForPath(): String = this
        .toLowerCase()
        .replace(" ", "_")
        .replace("#", "")

private fun pageFileName(pageNumber: Int) = if (pageNumber == 1) "index" else "page$pageNumber"*/
